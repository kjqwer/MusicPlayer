<Window x:Class="MusicPlayer.Views.LoadingDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="加载中" 
        Width="400" Height="150"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    
    <Border Background="#1A1A1E" CornerRadius="12" BorderBrush="#27272A" BorderThickness="1" Padding="24">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" x:Name="TitleText" Text="正在扫描音乐文件..." 
                       Foreground="#FAFAFA" FontSize="16" FontWeight="SemiBold" 
                       HorizontalAlignment="Center" Margin="0,0,0,16"/>

            <ProgressBar Grid.Row="1" x:Name="ProgressBar" 
                         Height="6" Minimum="0" Maximum="100" Value="0"
                         Background="#252529" Foreground="#6366F1" BorderThickness="0"/>

            <TextBlock Grid.Row="2" x:Name="StatusText" Text="0 / 0" 
                       Foreground="#A1A1AA" FontSize="13" 
                       HorizontalAlignment="Center" Margin="0,12,0,0"/>
        </Grid>
    </Border>
</Window>
