<Window x:Class="MusicPlayer.Views.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="输入" 
        Width="350" Height="160"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize">
    
    <Border Background="#1A1A1E" CornerRadius="12" BorderBrush="#27272A" BorderThickness="1" Padding="20">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock x:Name="PromptText" Grid.Row="0" Text="请输入:" 
                       Foreground="#FAFAFA" FontSize="14" Margin="0,0,0,12"/>

            <TextBox x:Name="InputBox" Grid.Row="1" 
                     Background="#252529" Foreground="#FAFAFA"
                     BorderBrush="#6366F1" BorderThickness="1"
                     Padding="10,8" FontSize="14"
                     VerticalContentAlignment="Center"/>

            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
                <Button Content="取消" Width="70" Height="32" 
                        Background="Transparent" Foreground="#A1A1AA"
                        BorderThickness="0" Cursor="Hand"
                        Click="Cancel_Click"/>
                <Button Content="确定" Width="70" Height="32" Margin="8,0,0,0"
                        Background="#6366F1" Foreground="White"
                        BorderThickness="0" Cursor="Hand"
                        Click="OK_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
